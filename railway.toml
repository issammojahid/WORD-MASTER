[build]
buildCommand = "npm install && node scripts/prod-migrate.cjs && npm run server:build && cp -r server/data server_dist/data"

[deploy]
startCommand = "npm run server:prod"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
